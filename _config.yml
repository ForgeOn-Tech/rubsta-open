# GitHub Pages (Jekyll) settings for the static landing page.
# web/ is the Node registration app. apps-script/ and tests/ support the waitlist
# form. None of them are part of the static site.
exclude:
  - web/
  - apps-script/
  - tests/
